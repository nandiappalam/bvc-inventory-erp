{
  "name": "bvc-erp",
  "version": "1.0.0",
  "description": "BVC ERP - Purchase Management System (Standalone Desktop Application)",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:frontend",
    "dev": "concurrently \"cross-env APP_ENV=local DB_ENGINE=sqlite SQLITE_DATABASE_DIR=database PORT=3001 node backend/server.js\" \"npm --prefix frontend run dev -- --host 0.0.0.0 --port 3000\"",
    "start:sqlite": "cross-env APP_ENV=local DB_ENGINE=sqlite SQLITE_DATABASE_DIR=database node backend/server.js",
    "start:postgres": "cross-env DB_ENGINE=postgres node backend/server.js",
    "start:local": "cross-env APP_ENV=local DB_ENGINE=sqlite SQLITE_DATABASE_DIR=database node backend/server.js",
    "start": "node backend/server.js",
    "test:isolation": "node backend/tests/company_isolation.test.js",
    "db:init-pg": "node backend/scripts/init-postgres-schema.js",
    "db:migrate-pg": "node backend/scripts/migrate-sqlite-to-postgres.js",
    "db:validate-pg": "node backend/scripts/validate-migration.js",
    "lint": "cd frontend && npm run lint",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "dotenv": "^17.4.2",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.13",
    "@types/node": "^25.9.2",
    "concurrently": "^9.2.4",
    "cross-env": "^10.1.0"
  },
  "author": "",
  "license": "MIT"
}
